name: CI Smoke Test Fixed
on:
  pull_request:
    branches: [main]
  push:
    branches: [main, feature/composite-actions]

jobs:
  lint-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Smoke test
        run: |
          echo "✅ Smoke test passed"
          python --version
          # ここに実際のスモークテストを追加
