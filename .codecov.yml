coverage:
  ignore:
    # ───────────────────────
    # ❶ tests 以下を完全に除外
    #    ** = 再帰ワイルドカード
    # ───────────────────────
    - "tests/**"

  status:
    patch:
      default:
        target: 80 # 目標
        threshold: 2 # ±許容差
