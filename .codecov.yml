---
coverage:
  status:
    project:
      default:
        target: 80 # 目標 80%
        threshold: "2%" # ±2 % までは pass
    patch:
      default:
        target: 80

comment:
  layout: "reach, diff, files"
  behavior: default
  require_changes: false

github_checks:
  annotations: true
